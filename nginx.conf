server {
	listen 9000;

	location / {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri /index.html;                 
    }

    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
